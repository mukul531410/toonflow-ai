# Codex Instructions

Before making changes, read the relevant files in `docs/`, which are the shared project source of truth. Inspect relevant files before modifying them and respect the active task scope.

Codex should prefer architectural and complex tasks when work is assigned, while avoiding unrelated refactoring and unapproved dependencies. Do not make architecture assumptions that conflict with the documentation.

After each implementation task, report files changed, testing performed, and known limitations. Stop after completing the requested task.
